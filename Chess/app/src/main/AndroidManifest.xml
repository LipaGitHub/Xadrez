<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.isec.tp.amov">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SinglePlayerActivity"
            android:label="You versus Enemy"/>
        <activity android:name=".AgainstPcActivity"
            android:label="You versus PC"/>
        <activity android:name=".LanguageOptionActivity"/>
        <activity android:name=".MultiPlayerMenuActivity"
            android:label="You versus Enemy (Network)"/>
        <activity android:name=".EatenPiecesActivity"/>
        <activity android:name=".ExistingGames"
            android:label="Saved games"/>
            <!--android:theme="@style/Base.Theme.AppCompat.Light.Dialog.Alert"-->
    </application>

</manifest>